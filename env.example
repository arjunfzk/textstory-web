# TextStory Web environment variables.
# Copy to .env.local for development.

# Puppeteer — path to system Chromium (set automatically in Docker)
PUPPETEER_EXECUTABLE_PATH=/usr/bin/chromium

# Node environment
NODE_ENV=development
